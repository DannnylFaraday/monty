#include "monty.h"
/**
 * stack_func - This prints the top
 * @top: stack head used in the program
 * @meter: line_number in the function
 * Return: no return
*/
void stack_func(stack_t **top, unsigned int meter)
{
	(void)top;
	(void)meter;
	bus.lifi = 0;
}
